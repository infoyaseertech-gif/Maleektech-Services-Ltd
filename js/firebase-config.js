// ============================================================
// FIREBASE CONFIG — fill in with YOUR project's values.
//
// Where to find these: Firebase Console → Project Settings (gear icon)
// → General tab → "Your apps" → select the web app (</>) →
// "SDK setup and configuration" → Config.
//
// These values are safe to expose in client-side code — Firebase's
// real security boundary is the Security Rules (see SETUP.md), not
// hiding this config. Do not skip setting up the rules.
// ============================================================

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
